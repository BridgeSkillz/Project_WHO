import React from "react";
//import logoImage from './project.png'; Import the logo image

const Home = () => {
  return (
    <div>
      <div className="paragraph2">
        <p>On an open ground, a motorist follows a track that turns to his left by
        an angle of 60° after every 500 m. Starting from a given turn, specify
        the displacement of the motorist at the third, sixth and eighth turn.
        Compare the magnitude of the displacement with the total path length
        covered by the motorist in each case.</p>
      </div>
    </div>
  );
};

export default Home;
